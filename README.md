#Para el correcto funcionamiento del programa se hace lo siguiente:
    - clonar el repositorio en su repositorio local--> https://github.com/anonymus95/segundaEntrega.git
    - Por consola escribe "npm install" sin las comillas
    - Para correr la aplicación se escribe en consola el siguiente comando "npm app" sin las comillas